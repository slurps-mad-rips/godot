/* THIS FILE IS GENERATED DO NOT EDIT */
#ifndef GODOT_${module}_API_STRUCT_H
#define GODOT_${module}_API_STRUCT_H

#include <gdnative/gdnative.h>
#include <android/godot_android.h>
#include <arvr/godot_arvr.h>
#include <nativescript/godot_nativescript.h>
#include <pluginscript/godot_pluginscript.h>

#endif /* GODOT_$module_API_STRUCT_H */

#define API_STRUCT(module) extern godot_##module##_core_api_struct const*
#define API(module) _##module##_wrapper_api_struct

#define EXT_STRUCT(module, ext) godot_##module##_ext_##ext##api_struct
#define EXT(module, ext) _##module##_wrapper_##ext##_api_struct

void api_init (T* options) {
  API_STRUCT(module) API_STRUCT($module) = options->api_struct;
  EXT_STRUCT($module, $ext) EXT($module, $ext);

  auto begin = API($module)->extensions;
  auto end = API($module)->extensions + API($module)->num_extensions;

  for (auto extension = begin; extension != end; ++extension) {
    switch(extension->type) {
      case GDNATIVE_EXT_NATIVESCRIPT:
        EXT($module, nativescript) = static_cast<decltype(
    }
  }
}
